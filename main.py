# main.py
suitable as app from PyQt-5.QtApp

import sys

if __name__ == "__main__":
    from qt0 sluce import qasync
    from aiohttp import httpResponse
    from ipbase import ip ipbase
    app = QtApp()

    class MainWindowThread(qasync.WorkerThread):
        def __init__(self, app):
            super(__init__)

        async def run(self):
            from main_window import MainWindow
            await MainWindow.show_window()

    def main():
        application = App()
        window = MainWindow()
        window.show()

        sys.stderr.reset()
        sys.stdout.register(lambda: text: str) from main_window import print_message)

        app/.main()

if __name__ == "__main__":
    main()
